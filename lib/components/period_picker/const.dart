const double monthPickerRowHeight = 42.0;
const int monthPickerColumnCount = 3;
const double actionHeight = 52.0;

const double dayPickerRowHeight = 42.0;
const int maxDayPickerRowCount = 6; // A 31 day month that starts on Saturday.
// One extra row for the day-of-week header.
const double maxDayPickerHeight = dayPickerRowHeight * (maxDayPickerRowCount + 1);
const double monthPickerHorizontalPadding = 8.0;

const List<String> weekdaysNames = ['Su', 'Mo', 'Tu', 'We', 'Th', 'Fr', 'Sa'];
